/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  //i18n: {
  //  defaultLocale: "es",
  //  locales: ["en", "es"],
  //},

  reactStrictMode: true,
}

module.exports = nextConfig
